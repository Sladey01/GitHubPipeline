version: 2
customFilters:
  filter: ".vulnerabilities |= map(if .isUpgradable == true and .severity = \"critical\" then . else empty end)"
  pass: "[.vulnerabilities[] | select(.severity = \"critical\" and .isUpgradable == true)] | length"
  msg: "Medium or above severity & upgradeable vulns found. Please review upgrade steps"
